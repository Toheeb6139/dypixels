// Update this if/when a custom domain is connected — everything that
// needs an absolute URL (metadata, sitemap, robots.txt) reads from here.
export const SITE_URL =
  process.env.NEXT_PUBLIC_SITE_URL || "https://dypixels.vercel.app";
